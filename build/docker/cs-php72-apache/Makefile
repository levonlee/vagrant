.PHONE: info dps drc dbuild

info:
  cat Makefile

edf:
  nano Dockerfile

dps:
  sudo docker ps -a

drc:
  sudo docker rm $(c)

dimg:
  sudo docker images

dbuild:
  sudo docker build -t cs-php56-apache .
